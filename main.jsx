import React from "react";
import ReactDOM from "react-dom/client";
import Elsewhere from "./travesite.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Elsewhere />
  </React.StrictMode>
);
